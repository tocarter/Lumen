# Lumen mobile

Capacitor shell for iPhone and Android. It loads the same Next.js portal as desktop.

Set `LUMEN_MOBILE_SERVER_URL` in `mobile/.env`, start the portal with `npm run dev:mobile` from `web/`, then `npm run sync`.
